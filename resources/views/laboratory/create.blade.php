@extends('layouts.app')
@section('title','Nova coleta • ETEFlow')
@section('content')
<div class="app-shell">@include('operation.navigation')<main class="workspace"><header class="page-header"><div><span class="eyebrow">LABORATÓRIO</span><h1>Nova coleta</h1></div></header><form method="POST" action="{{ route('laboratory.store') }}" class="panel-card stack-form">@csrf<input type="hidden" name="operational_unit_id" value="{{ $unit->id }}"><div class="form-row"><label>Origem<select name="origin_type"><option value="INTERNAL">Interna</option><option value="EXTERNAL">Externa</option></select></label><label>Ponto<select name="sampling_point_id">@foreach($points as $point)<option value="{{ $point->id }}">{{ $point->name }}</option>@endforeach</select></label><label>Coleta<input type="datetime-local" name="collected_at" value="{{ now()->format('Y-m-d\TH:i') }}"></label></div><div class="lab-result-grid">@foreach($parameters as $parameter)<label class="field"><span>{{ $parameter->name }} ({{ $parameter->default_unit }})</span><input type="number" step="any" name="results[{{ $parameter->id }}]"></label>@endforeach</div><button class="button primary">Salvar coleta</button></form></main></div>
@endsection