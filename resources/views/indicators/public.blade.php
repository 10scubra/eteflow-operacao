<!DOCTYPE html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $share->name }} • ETEFlow</title><link rel="stylesheet" href="{{ asset('assets/app.css') }}"><script type="module" src="{{ asset('assets/app.js') }}" defer></script></head><body data-page="public-indicators"><main class="public-dashboard"><header class="public-hero"><div><span class="eyebrow">PAINEL COMPARTILHADO</span><h1>{{ $share->name }}</h1><p>{{ $share->operationalUnit->name }} • somente leitura</p></div></header><nav class="indicator-tabs">@foreach($share->pages as $option)<a class="{{ $page->id===$option->id?'active':'' }}" href="{{ route('public.indicators.show',['token'=>$token,'page'=>$option->slug]) }}">{{ $option->name }}</a>@endforeach</nav><section class="indicator-grid">@forelse($cards as $card)<article class="panel-card indicator-card"><h2>{{ $card['indicator']['name'] }}</h2>@if($card['state']==='NO_DATA')<div class="empty-state">Sem dados no período</div>@elseif($card['indicator']['visualization']==='KPI')<div class="indicator-kpi">{{ $card['value']===null?'—':number_format($card['value'],2,',','.') }} <small>{{ $card['indicator']['unit'] }}</small></div>@else<div class="indicator-chart" data-series='@json($card["series"])'><svg viewBox="0 0 600 220"></svg></div>@endif</article>@empty<div class="empty-state">Nenhum indicador autorizado.</div>@endforelse</section><footer class="public-footer">ETEFlow • link seguro somente leitura</footer></main></body></html>